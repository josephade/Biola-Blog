---
// library imports
import { Icon } from "astro-icon/components";

// component imports
import Link from "./Link.astro";

// data imports
import navData from "../data/navData";

---

<nav aria-label="Primary">
  <div class="navbar">
    <a href="/" class="logomark" aria-label="Go home">
      <Icon name="logomark" width="60" />
    </a>
    <ul>
      {
        navData.map((item) => (
          <Link
            text={item.name}
            href={item.path}
            style="primary"
            isFilled={false}
            data-navLink
          />
        ))
      }
    </ul>
    <!-- <div id="cta" class="" style="display: flex;"> -->
      <!-- <Link
        href="/settings"
        id="settings-link"
        text="Profile"
        style="primary"
      /> -->
      <!-- <Link
        href="/signup"
        id="signup-link"
        text="Sign Up"
        style="primary"
      /> -->
      <a class="cta hidden" href="/settings" id="settings-link">Profile</a>
      <a class="cta" href="/signup" id="signup-link">Sign up</a>
    <!-- </div> -->
    <!-- {
      user ? (
        <div class="">
          <Link href="/settings" text="Profile" style="primary" />
        </div>
      ) : (
        <div id="cta" class="">
          <Link href="/signup" text="Sign Up" style="primary" />
        </div>
      )
    } -->
  </div>
</nav>

<script>
  import "../js/nav.js";
</script>
